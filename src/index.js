import { player, enemy, randomEnemy, gameState, endGame, switchPlayer, attack, heal, flee, playGame } from "./js/RPG.js";

window.onLoad = function() {
  document.getElementById('start').onClick = (event) => {
    event.preventDefault();

    const attack = document.getElementById("")
  }
}

playGame()

